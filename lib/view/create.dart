import 'package:flutter/material.dart';

class ViewCreate extends StatelessWidget {
  const ViewCreate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('create'),
      ),
    );
  }
}
